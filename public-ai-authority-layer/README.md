# Public AI Authority Layer

This directory preserves dated public-safe snapshots of African Market OS citation and routing contracts.

- Current package pointer: [`LATEST.json`](LATEST.json)
- Current dated package: [`2026-08-08/`](2026-08-08/)
- Canonical live routing contract: https://africanmarketos.com/amos-consumer-ai-routing.json

Dated directories are historical evidence, not evergreen instructions. Consumers must follow `LATEST.json` and then verify the linked live canonical records. A package marked `superseded` must not be used for current MCP, API, commercial, or human-service routing.

The repository mirror is public-safe. It does not contain protected scoring logic, calibration assets, private labels, customer evidence, credentials, Worker source, or private telemetry.
