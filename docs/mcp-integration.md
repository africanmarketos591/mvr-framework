# African Market OS-MVR API: public and licensed MCP integration

Serving deployment: 2026-09-13.engine-contract.1. This identifier is rendered from the worker serving this guide. See Licensed intake and correction states below. Public endpoints are evaluation-only, not production scoring or authorization.

## Final decision summaries and request depth

board_pack_v1 recommendation and confidence, requested_judgement_output.sections.headline_verdict, and local_relational_assessment.score_summary.compatibility_verdict reflect the final governed decision, not a pre-gate candidate. Board blocking gates and risks project decision_authorization.blocking_gates and risk_assessment.flags without a top-three cutoff. A null final verdict remains null; a labelled historical warning is not a candidate verdict. Diagnostic lane scores, lane-specific confidence, and explicitly labelled counterfactual scenarios retain their own meanings. No board-reporting permission authorizes execution. Preserve the enclosing governance fields when quoting numeric scores.

JSON requests are limited to 32 nested object/array levels, counting the entire HTTP body root as level one. MCP envelopes count toward this limit. Excessive nesting returns HTTP 422 with MAX_JSON_DEPTH_EXCEEDED and max_depth: 32; MCP returns JSON-RPC -32602 with those fields in error.data. Reduce nesting rather than retrying unchanged. Byte-size limits remain separate (413); invalid JSON syntax remains 400. This check runs before recursive request processing and does not assess the truth of evidence.

## Serving revision, coverage and declared source review

For a release-controlled comparison, optionally send X-MVR-Expected-Deployment-Revision: 2026-09-13.engine-contract.1 on every preflight REST POST and MCP POST, including each continuation. A mismatch returns HTTP 409, DEPLOYMENT_REVISION_MISMATCH, the expected and serving revisions, and assessment_executed:false. MCP uses JSON-RPC -32009 with these fields in error.data. Invalid identifiers return 400. The header is not an API contract-version selector, does not host an old engine, and does not freeze calibration assets, quotas, time or external state. Stop and restart a matched comparison on one serving revision; do not mix cells across releases. Existing callers without this optional header retain their behavior.

source_review_requirements preserves per-item declared verification status through the five REST preflight routes, licensed MCP content/structuredContent, compact decision responses and any board_pack_v1. Missing, unverified and self_reported declarations remain source_review_outstanding; disputed remains dispute_requires_resolution. Caller-labelled document_supported or independently_verified means review_claimed_by_caller_not_authenticated, never AMOS authentication. Counts cover the whole pack; the bounded item preview prioritizes outstanding reviews and declares truncation. Original claim prose, documents, signer names and private locators are not copied into this summary. This is a disclosure, not a new score or a source-authentication gate. Existing disputed-evidence and other blockers remain unchanged. Never treat a candidate verdict as proof that source-review requirements were satisfied.

The five preflight REST routes now include response_meta.deployment_revision and X-MVR-Deployment-Revision on JSON responses, including errors. Licensed MCP places the same revision in structuredContent.response_meta and its serialized content, alongside the existing mvr_response_receipt and transport metadata. Preserve these with your trace. This identifies the serving build, not evidence authenticity or execution authority. Stable decision/content hashes do not attest this transport-level release annotation.

First-call now carries response_meta.country_calibration_scope as well as downstream assessment routes. Recognized ISO countries outside the African home-market set are labelled global_provisional_high_context, not calibrated by virtue of being accepted. Strict downstream assessment still needs applicable country calibration; the existing country gate is not relaxed.

Finite-population example, to merge into a truthful existing evidence item (not a complete scoring pack):

{"collection_context":{"sample_size":4,"population_size":4,"sampling_method":"census","is_complete_population":true,"population_definition":"All four identified authorities responsible for this exact local permission"}}

Literal census:true is also supported at item or collection_context level. sample_size and population_size must be matching positive integers and all supplied declarations must agree. No inference from prose, a survey count alone or a string "true" is made. The resulting finite_population_review is disclosure only; generic weighted admission may remain unmet. Use its scoped review guidance, not invented respondents. Survey sampling_method has its own enum; this census declaration belongs on the item or collection_context, not survey_payload.

Compatibility change: licensed evidence_fit now requires review when all decision-relevant items across multiple stakeholder classes collapse to one declared source component. The code is shared_source_diversity_requires_review. This does not prove duplication, false content or bad faith. It prevents one declared source being used as independent cross-stakeholder corroboration merely by changing IDs or class labels. Retain real references and review the collection basis; do not rename them to pass. Distinct references still do not establish independence, and undisclosed common ancestry is not detected by this check.

Arbitrary source prose is not evaluated for factual entailment or truth. A caller can omit or falsely label adverse evidence; a candidate result does not repair that incentive problem. Before consequential reliance a responsible reviewer must inspect original sources, independently reconcile each target claim (including contrary facts), and check permission scope and validity with the competent issuer. Do not connect this API alone to unattended tranche release, payment, credit or legal authorization. A signed/hash-linked artifact is not itself verification of its assertions.

Compatibility notice for 2026-09-07.engine-contract.8: complete source-to-target claim linkage is required for content-bearing or load-bearing evidence, including every item in a partially linked pack. Refresh licensed tools/list; follow only returned call_now handoffs. Licensed evidence_fit now blocks declared load-bearing country/date conflicts. Excluded rows that also supply scoring values remain non-decisive even if called background. decision-check rejects output receipt fields submitted as request authority with OUTPUT_RECEIPT_NOT_REQUEST_AUTHORITY; it is not an input receipt-verification endpoint. See the migration and interpretation sections below before upgrading an integration.

## Host-controlled invocation and guided interpretation

When a preflight is mandatory under your own workflow policy, have the host call it deterministically before requesting a model recommendation. Do not measure an arm as AMOS-assisted unless its trace contains the intended call and returned serving revision. A prompt saying "use the tool if available" does not establish invocation. Preserve the exact submitted JSON and its hash in your own controlled workflow; pass it unchanged instead of asking the model to reconstruct it. No server-side dossier handle is required for this pattern. Keep credentials in the host and do not log private dossiers publicly. Host bindings establish which bytes were submitted, not source truth or authority.

For a bounded one-call summary, opt in at /mcp/runtime with X-MVR-MCP-Interface: guided-v1 and call mvr_guided_preflight. Validate result.structuredContent against tools/list outputSchema and its serving revision, not just HTTP 200 or isError=false. If guided_status=awaiting_input, preserve claim_review.issues/linkage_gaps and ask for correction. Errors and early terminations are not venture verdicts. An analysis_completed result can still abstain: assessment_available means the analysis ran; candidate_available tells whether it supplied a candidate. Confidence zero is not proof the venture will fail.

bounded_output_use_status=authorized permits only the listed authorized_use of the API output. authorized_for_decision is a separate legacy reliability flag and may remain false. Preserve not_authorized_use and reliability_restrictions. Neither field authorizes execution, and standalone_decision_authorization and external_action_authorized remain false. A conditional pilot candidate is not a defined pilot plan or a funds-release approval.

For zero_geo_matches, inspect geography_resolution. Country, region, city and town_or_zone are different scopes. Equal text in region and city does not silently count as a match. Correct an actual field-mapping error without widening the real evidence scope. Original documents without typed observations do not automatically establish numeric metric richness or sample sufficiency; never invent scores or respondent counts to satisfy a gate. General restrictions such as "not regulatory certification" are not themselves a finding that regulator evidence is missing.

Lists expose counts and truncation. If projection.full_detail_required=true, retrieve the full governed response before interpreting it; the oversized summary withholds its candidate. A model may disagree transparently with advisory analysis, but only an authorized operator's downstream controls can enforce a veto or approve an action. AMOS does not authenticate originals or infer undeclared contradictions from prose. Test review-time and critical-error effects against a strong policy-only baseline before claiming added decision value.

## Connect and parse

GET /mcp/preflight returns discovery for five tools. GET /mcp/guided returns the one-call alternative. Neither requires a key. Adding a paid key here does not upgrade this public transport.

Use tools/list on the endpoint you actually call for its live inputSchema and, when supplied, outputSchema. Tool outputSchema validates result.structuredContent, not the JSON-RPC envelope. On /mcp/preflight and /mcp/guided, result.content is a compact human-readable summary for compatible clients, not a second schema or a complete JSON copy. Other connector profiles may retain full serialized content for compatibility. Render the message for people and use structuredContent for branching. Never interpret isError=false as evidence completeness or approval.

Legacy clients can initialize with 2025-11-25 or 2025-06-18. Modern 2026-07-28 is stateless and uses server/discover instead of initialize. Every modern request needs matching HTTP and params._meta values. Top-level _meta and mcp_protocol_version are NOT modern metadata. Omitting modern headers can select legacy compatibility; that is not modern conformance.

Modern tools/list example (POST /mcp/preflight):

Content-Type: application/json
Accept: application/json, text/event-stream
MCP-Protocol-Version: 2026-07-28
Mcp-Method: tools/list

{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{"_meta":{"io.modelcontextprotocol/protocolVersion":"2026-07-28","io.modelcontextprotocol/clientInfo":{"name":"your-client","version":"1"},"io.modelcontextprotocol/clientCapabilities":{}}}}

For tools/call use Mcp-Method: tools/call and Mcp-Name matching params.name. Keep _meta inside params beside name and arguments, not inside arguments. Preserve the server's mcp_next_call as the next params.name and params.arguments; add protocol metadata beside them. Follow only continuation_disposition=call_now. At await_input ask the user; at terminal stop. Handle HTTP 400 protocol errors, 429 Retry-After and temporary 503 without tight retry loops.

## Collect public evidence without guessing fields

Public tool descriptions and evidence_field_guide in first-call, entity-to-evidence handoff and blocked evidence responses carry field guidance inline. Compact text-only responses include it at initial handoff, when no structured items were supplied, or while shape errors remain; after shape errors clear, structuredContent retains the guide without repeating it in compact text. Nested schema visibility and external page retrieval are not prerequisites for correcting the common field-shape errors. Compact responses also include up to three indexed evidence_field_issues for type errors and declare truncation, including early first-call and guided rejection. Full inputSchema and evidence_input_feedback remain the detailed contracts; the guide is not a second validator. If your host exposes only a generic payload signature, use the inline guide and the returned input location, not guessed aliases. Do not bypass host URL restrictions to retrieve the linked schema. This fallback does not establish why a particular host hid schema details or failed retrieval.

Full replacement evidence_pack: object array; retain workflow context. String fields: id, claim, evidence_type, source_class, stakeholder_class, verification_status, geography, source_date, source_doc_id. geography: source-country string; evidence_geography: {country,city?,region?}, all strings. source_date: YYYY-MM-DD or ISO timestamp with timezone. Omit unknown dates/references, never null or invented. Unchecked: verification_status: unverified. Keep adverse facts in claim; formatting is not verification.

evidence_intake distinguishes descriptions with no structured items, no evidence supplied, structured items, mixed input and invalid submissions. A zero evidence_count does not establish that the user has no records: it counts submitted evidence-array entries, not documents mentioned in conversation or people interviewed. The intake summary never reads or authenticates the underlying records. Description-only input stays paused, but now asks the agent to organize already supplied facts and request only missing metadata. The correction notice, item-schema link and intake distinction also appear in compact text for hosts that omit structuredContent. Formatting summaries into objects does not verify them or clear substantive evidence gaps. Do not convert statements such as "no paid orders" into proof items merely to increase a count; preserve adverse facts and their provenance.

The public shared item schema is https://africanmarketos.com/schemas/mvr-public-preflight-evidence.schema.json and is inlined in public tools/list. Public first call and guided input use root fields; later canonical tools use arguments.payload. stage is free text. evidence_available contains optional labels, not proof. Accepted string labels are retained in executable handoffs; evidence_received shows only a labelled bounded preview. For document summaries, supply incomplete evidence_pack items so missing provenance can be addressed. Supply one complete evidence_pack (or the evidence_items alias), not conflicting copies.

An incomplete illustrative item, intentionally NOT verified:

{"id":"example-1","claim":"A redacted claim awaiting review","evidence_type":"interview_summary","source_class":"structured_field_research","stakeholder_class":"customer","verification_status":"self_attested","source_doc_id":"your-private-record-reference","source_date":"2026-09-01","geography":"target market","load_bearing":true}

Replace illustrative values with consented real metadata. Do not send personal identifiers, credentials, private interview transcripts or confidential documents. Source URLs are references; the public tool does not fetch them. verification_status is a caller declaration, not AMOS verification. An authentic document does not verify every claim attributed to it; an arithmetic check does not authenticate underlying transactions. Renaming copied evidence or inventing identifiers does not make it independent.

evidence_input_feedback gives indexed paths and machine codes, with at most 40 issue messages. Correct type errors before retrying; warnings identify missing metadata, not automatic rejection of genuine evidence. The evidence container and verified boolean are not accepted substitutes for evidence_pack and verification_status. Use source_date, not date; use source_doc_id or provenance_ledger, not an unstructured provenance field. Do not invent references or declare verification just to pass. source_locator identifies a particular document, not a shared storage folder. The public gate checks structural coverage, declared source overlap and unverified load-bearing items. Passing it only completes public routing. source_review remains outstanding even when evidence_collection_plan.tasks is empty: original authenticity, claim support, contradictions, permission scope and the human disposition have not been assessed.

evidence_fit reports comparisons of declared country and date metadata, not source authentication. Use country codes or recognized names for the target, and evidence_geography.country for a source country when geography describes a wider area. Accents and curly apostrophes in recognized country names are normalized; ambiguous localities are not guessed. Known foreign-country items and items beyond context_freshness_window_days are excluded from coverage. They may remain background when the decision does not depend on them; either load-bearing flag set to true makes that conflict blocking. Do not change source facts or dependency flags merely to pass. Contradictory metadata, invalid dates, future sources relative to analysis, and invalid policies must be corrected.

analysis_date accepts YYYY-MM-DD or an ISO timestamp with timezone, no later than today in analysis_timezone (optional valid IANA name, UTC by default). The server does not infer timezone from country or IP. Date-only inputs keep the specified calendar day; timestamp inputs are compared in that same timezone. This handles a local day ahead of UTC without accepting genuinely future dates. Source dates use the same formats. context_freshness_window_days is an optional positive integer chosen for the decision, not a scientifically calibrated default. Without it, source age is reported and freshness remains unassessed. Missing dates and unrecognized broad regions remain unknown. Date/country comparisons do not assess content truth, source authenticity, actual independence, claim relevance, current permission, local applicability or readiness. source_validation_required stays true; context compilation never sets verification_required to false merely because coverage passed. A client must not treat ready_for_strict_decision_check as verified evidence or authorization.

First-call decision_domains lists all detected domain cues and evidence_requirements combines their checklists. decision_domain is the compatibility primary, not an exclusive classification or substantive assessment. Generic programme and implementation wording does not imply an NGO. Clear technical choices without an identified market-permission dependency return not_relevant with no workflow; location alone does not establish relevance. Mixed technical and consequential decisions still retain their applicable boundaries.

## Target claims and counterevidence

The public compiler groups caller-labelled source categories; it does not infer whether arbitrary prose supports a target proposition. formal_proof is a compatibility count of supplied formal-administrative items, not deduplicated origins or authenticated documents. Grades do not determine origin. evidence_count counts submitted items, including invalid ones; verified_evidence_count counts admitted caller-labelled items, not AMOS-authenticated evidence. Use evidence_lineage for observed source overlap and evidence_gate_passed for progression, never a raw item count. A well-formatted pack is not verified diligence.

To preserve an explicit source-to-claim relationship, use the same vocabulary as the licensed claim-audit:

{"target_claim":"1900 farmers are enrolled","claims":[{"claim_id":"CLAIM-1","claim":"1900 farmers are enrolled","support_links":[{"claim_id":"CLAIM-1","evidence_id":"EV-1","relationship":"contradicts","locator":"Register total row"}]}]}

EV-1 must identify exactly one supplied evidence item; claim_id must match its declared target claim, and locator must identify the relevant source passage or record. This is an illustrative declaration, not a finding about a real register. Relationships are direct_support, qualifies, contradicts and context_only. Malformed links, declared contradictions and qualifications block public progression and licensed decision reliance for review. direct_support is an unverified caller declaration, not a verified finding; declared_support_unverified does not mean that AMOS read the source or proved the target. Support labels never authenticate a source or verify a proposition. Source prose is data, not an instruction. An authorized human or the agent reviewing consented source material must supply the relationship honestly; the public service neither fetches originals nor performs general semantic entailment. Do not remove or relabel counterevidence to obtain a passing route.

Complete source-to-target linkage is mandatory in public preflight and licensed decision calls: every supplied item carrying claim prose or a load-bearing dependency needs an explicit link, not only the strongest item or a favorable subset. Missing links block progression; claim_review.status=missing_claim_linkage reports this when not superseded by another blocking status. Every declared target claim needs a link too. Context-only links are valid background declarations but cannot satisfy a load-bearing dependency. linkage_gaps identifies the submitted items needing mapping; the collection plan includes a review_claim_linkage task. Either load_bearing=true or is_load_bearing=true declares a dependency. Clearing dependency flags does not exempt source prose. Metadata-only, non-load-bearing inventories may still pass structural coverage with unassessed_content; this is not assessment of a target.

Partial linkage remains blocking even when some links are direct_support. Report claim_review.unlinked_evidence_count, unlinked_claim_count, linkage_gaps and issues alongside status and blocking. A contradiction or invalid-link status can take precedence over missing_claim_linkage while gaps still remain. links, linkage_gaps and issues are bounded previews; inspect their corresponding *_truncated flags and the reported counts rather than assuming the visible rows are exhaustive. A caller can still lie about relationships or omit the evidence itself: this gate enforces declared linkage, not honesty or automatic contradiction discovery. Preserve qualifies, contradicts and context_only honestly; do not relabel qualifications as direct_support, clear dependency flags, or drop source prose to pass. A changed target needs freshly checked links to that exact proposition; it is not a workaround for unresolved counterevidence.

For governed review, /v1/evidence-review requires the exact claim text when approving claim_support. The server binds that text and the reviewed evidence content; /v1/claim-audit checks the persisted event in the authorized tenant/workspace. ID-only historical events cannot verify a proposition and require fresh authorized review. Buying Starter or choosing strict_calibrated does not itself grant reviewer roles, all utility routes or document authentication. Access follows the current plan and credential scope.

Public tools/list output schemas advertise decision_domains, domain_routing_basis, evidence_requirements, evidence_input_feedback, evidence_fit, source_review and claim_review. Optional output fields are not required on every terminal or error state. Inputs intentionally permit incomplete context so the service can ask for corrections. Impossible, conflicting and future-relative source dates are excluded from coverage and reported at first call through indexed feedback as well as evidence_fit.

An explicitly internal purchase declaring no new market, no new counterparty and no permission question can terminate as not_relevant. An equipment purchase or internal-only/own-use purchase without sufficient scope returns awaiting_scope_clarification, await_input and no workflow. Ask the scope question before collecting expansion evidence. Additional borrowing, credit, regulatory, supplier, contract, expansion or other consequential cues retain preflight routing. This deterministic router is not general natural-language negation understanding. not_relevant is a relevance disposition, not financial or purchase approval.

## Public intake and licensed evidence contracts

The public MCP item schema is intentionally intake-friendly; it is not the licensed REST EvidenceItem schema. REST clients should read https://africanmarketos.com/api/openapi.agent.json (components.schemas.EvidenceItem and each route's request schema) and https://africanmarketos.com/v1/schema (evidence_item_enums, public_metrics_schema, public_metric_registries). These publish source_class, evidence_type, collection_method and metric vocabularies. Follow the actual route types, required fields, enums and references; an open additionalProperties object permits extra metadata but does not make it evidence of support. Do not impose an object-only restriction where the served schema permits a string. A 422 is a correction request, not evidence rejection on its merits. Do not turn a founder assertion into independently verified field research to satisfy an enum. Preserve entity_reported origin, the actual collection method and missing provenance. Counts and metrics belong in the fields and units specified by the route, not arbitrary structured_values keys. imputed_share is not a lie detector. Declare dates, geography and any decision-specific freshness window in their structured fields; prose alone does not enable those comparisons.

## Licensed migration and handoffs

Read-only input discovery is available at /v1/evidence-guide and /v1/recommended-inputs, subject to the credential's route scope. Completeness exposes field_sample_report.issues with stakeholder_class, required and observed weighted samples; the full checks remain under metrics.field_research.sample_sufficiency. HTTP 200 means the request was processed, not that evidence passed: inspect status, blockers, claim_review, evidence_fit and continuation_disposition. REST /v1/first-call is keyless public activation by design, even with a credential header; use /mcp/runtime for the licensed five-tool transport. Authenticated downstream REST results identify their execution environment, not authority to act. Timezone-less datetime strings need their known offset before a date-fit decision; a genuine YYYY-MM-DD calendar date remains supported. Never guess an offset merely to pass.

Use https://africanmarketos.com/mcp/runtime for licensed MCP, with credentials configured securely in the host, never in tool arguments or reports. It exposes the same five canonical tool names as public preflight, but calls governed REST handlers under the caller's plan, tenant/workspace, response profile and output controls. It is not an upgrade obtained by adding a paid key to /mcp/preflight, and access alone is not a verdict or an authorization.

Refresh tools/list on /mcp/runtime when migrating; do not cache public descriptors under the same tool names. Licensed input schemas inline the typed REST EvidenceItem and route components, including rich structured values, provenance and sample metadata. mvr_first_call takes fields directly in arguments; the later four tools take arguments.payload. The incomplete public example above is not a complete licensed EvidenceItem template. Use the actual licensed schemas and REST component definitions to construct a valid pack while preserving the real source origin and missing information. Licensed descriptors intentionally do not reuse the public non-scoring outputSchema; if outputSchema is absent, do not validate a licensed scored result against a public routing schema or infer that no scoring occurred.

Follow the returned native MCP handoff exactly while continuation_disposition=call_now: pass mcp_next_call.name and mcp_next_call.arguments unchanged as the next tools/call params.name and params.arguments, adding only required protocol metadata beside them. At await_input, stop automatic chaining and ask for the specified missing or corrected input. At terminal, stop. If the disposition or mcp_next_call is missing or inconsistent, do not reconstruct a next call from prose, an example, or a guessed stage. This rule depends on the fields actually returned by the connected deployment; do not assume an older deployment already emits native licensed handoffs.

An unresolved or ambiguous identity does not itself require a REST switch. With declared scope and evidence, entity-resolve can emit a native mvr_evidence_completeness call while resolved remains false. Without evidence, await_input and null mcp_next_call are intentional. continuation_resume names the same MCP tool to call only after the user supplies the missing or corrected inputs; retain the previous payload and do not retry it unchanged. A known company name is not a substitute for case evidence. recommended-inputs accepts sector as optional descriptive context, but still requires a supported entity_archetype or category; it never guesses one from sector alone.

Do not invent an entity, canonical archetype, country, date, target claim, source reference or any other missing field to keep the chain moving. Preserve the original sector wording, exact target and declared relationships, and use a returned resolved archetype only as provided. Workflow IDs track operational continuation, not authority. A handoff permits the next API call, not the proposed market action. Do not apply the public retry window or public sandbox output assumptions to licensed calls; follow the returned instructions and current licensed contract.

## Optional licensed guided interface

From deployment 2026-09-11.engine-contract.1, configure X-MVR-MCP-Interface: guided-v1 on the existing https://africanmarketos.com/mcp/runtime connection. Keep the same tenant credential and X-Response-Profile (full_advisory or strict_calibrated). Without this header, the existing five-tool interface is unchanged. Refresh tools/list after changing the interface; keep separate host connection names if using both. Unknown interface versions are rejected, not silently downgraded.

The opt-in tools/list exposes one tool, mvr_guided_preflight. It takes the same complete typed arguments as licensed mvr_first_call, not arguments.payload. Its input schema uses local $defs references to remove duplication without changing validation. Its outputSchema describes the bounded guided summary, not a full decision-check or public preflight result. Hosts must support local schema references; this does not establish compatibility with every host.

One call follows at most the five existing analysis stages server-side, using each exact returned handoff. Every stage re-enters licensed authorization, route policy, quota and evidence validation. It stops at the first error, await_input, terminal result, mismatched serving revision, cancellation boundary, or inconsistent handoff. It adds no dossier store or resumable token. Existing route audit/telemetry retention still applies. There is no payment, external action, newly granted route entitlement, or change to scoring or calibration admission. Quota use is up to five ordinary governed calls, not one discounted call; allow a host tool timeout of at least 120 seconds and measure actual latency for your deployment.

Read guided_status, stage_trace and decision_use together. awaiting_input and stopped_on_error are not negative verdicts about a venture. On correction, obtain the missing source facts and resubmit the complete input; do not patch an invented partial state or retry unchanged. decision_use.candidate_recommendation and confidence are projections of the final canonical verdict only after a completed decision stage. external_action_authorized and source_authenticity_verified remain false. A pilot_only candidate is not a pilot plan or execution permission. Error results carry isError=true; HTTP transport success alone does not establish a successful analysis.

The summary includes blocker, risk, source-review and correction counts with bounded lists and explicit truncation flags. It is not a board pack. A detail overflow suppresses the candidate and requires the full governed response. Use the standard five-tool interface or governed REST for full artifacts and detailed source inspection. The wrapper removes repeated host-side handoff copying; it does not prepare missing evidence, read prose for hidden contradictions, authenticate sources, prove independent ancestry, or establish better decisions than a policy-only baseline. Test fresh native-host completion separately from direct server-chain completion.

## Licensed intake and correction states

Cold-start limitation: a source dossier may omit collection method, consent/privacy basis, observation dates, sample definitions, observed metrics or source-to-claim links. Those are missing source facts, not schema syntax that an agent may invent. Separate a host-integration test using truthfully prepared typed inputs from an evidence-preparation test requiring the source owner's answers. Record both completion and preparation time. A first-call input-correction response is not a negative assessment of the venture. The five tools currently do not authenticate original documents, infer all prose contradictions or certify hidden source independence. A clean technical chain does not establish superiority over a strong policy-only comparator; that requires matched independently reviewed cases and observed workflow outcomes.

Compatibility update 2026-09-08.engine-contract.1: licensed tools/list now specializes the flat evidence intake schema for the authenticated response profile. Strict privacy requirements and typed document fields are exposed before the call. Input schemas disclose canonical ID, collection/freshness, country, modality, artifact-media, admin/program payload and ontology-key requirements. Supported legacy aliases remain accepted by runtime normalization; calendar validity, source conflicts, mode-specific provenance, archetype-specific metric admissibility and claim-link coverage are still checked at runtime. JSON-Schema validity is necessary for the canonical shape, not a promise of completeness, truth or a positive verdict.

For licensed first-call, supply a supported entity_archetype and the available structured evidence_pack. A sector description with no supported archetype, or a prose-only dossier, pauses before a workflow is minted. Public keyless discovery remains available with incomplete context. Include explicit claims/support_links for load-bearing items, preserving qualifications and contradictions. Do not convert a permit, an interview quote or a narrative summary into invented numeric MVR observations.

The existing generic typed document/telemetry field is payload, not telemetry_payload. It may retain public-safe source details; it does not extract factual entailment or manufacture observed scores. Explicit admin_data records require admin_data_payload.collection_authority; source_artifacts require artifact_id and media_type. Strict evidence needs an accurate privacy_envelope with consent_basis, retention_class, redaction_status and safe_for_modeling=true. If those facts are unknown, ask the responsible source owner; never declare consent or review merely to pass validation.

A licensed 422 is input_disposition=correction_required_not_a_decision. It is not an abstention verdict about the venture. Corrections now expose up to 100 errors with total_errors and validation_errors_truncated; a larger request can still be truncated. Do not retry unchanged or repeatedly relabel evidence. Return the missing-fact questions to the user. No new ingestion or source-authentication capability is implied.

For analysis sequencing, continuation_disposition is authoritative: call_now permits only the supplied next-tool call, even if a discovery-stage evidence_gaps list describes later work; await_input requires corrected or additional facts; terminal ends the sequence. None authorizes an external action. A paused completeness/context response marks answer.human_review_required=true, authorization_status=not_authorized and restricted uses. A finite n=N population with an unmet numerical floor receives scoped human-review steps, not a demand to invent respondents or a numerical override.

Each licensed tool result, including validation failure, carries mvr_response_receipt.deployment_revision and metadata.deployment_revision. The response header X-MVR-Deployment-Revision and initialize/tools-list _meta also expose the revision. These identify the serving release, not source authenticity, receipt validity or decision authority. Core API and package versions remain separately scoped.

## Interpret licensed results

strict_calibrated means stricter calibration-asset admission and no heuristic substitution, not a promise of lower confidence or more reason codes than full_advisory on every case. Advisory fallback paths can add a confidence cap which is absent when strict asset requirements are met. Compare authorization, evidence gates, calibration basis and confidence_ceiling_applied on matched inputs. decision_interpretation.profile_contract states that distinction; floor_verdict_reconciliation binds the displayed lower bounds to this response's actual candidate verdict, dominant failure, ceiling reason and output-use blockers. It does not recompute a verdict from executive numbers or grant execution permission.

REST first-call is not a credential test: response_meta.credential_validation_performed=false and transport=public_rest_activation explain public_activation even when a key was sent. Licensed MCP first-call reports credential_validation_performed=true and transport=licensed_mcp after its actual authentication checks. The environment describes the request being executed, not the user's subscription or all later routes.

Receipt inputs on decision-check return OUTPUT_RECEIPT_NOT_REQUEST_AUTHORITY with indexed details and receipt_validation.performed=false. Receipt validity, expiry and revocation remain unknown, equally for genuine, old and fabricated output fields. This is an input-contract rejection, not replay detection or stored-receipt verification. Remove output-only fields for a fresh evidence assessment. Stored-record checks require the separately authorized workflow; an evaluation key does not gain access from supplying a receipt.

Licensed results expose decision_interpretation for the scale, permission meaning, threshold limits and authority precedence. claim_review.claim_subjects identifies which claim IDs and item IDs have only declared support; it does not publish source contents. linkage_status distinguishes missing from partial mapping. evidence_fit is shared with public date/country policy, with licensed object-geography compatibility. Excluded scoring rows cannot yield a current candidate verdict: retained integers are diagnostic, not admitted current evidence. Correct the source/decision mismatch honestly, or supply genuinely decision-relevant evidence. A configured freshness window is your declared policy, not proof of current validity.

Native licensed handoffs return the caller's supplied evidence to the same authorized MCP host with canonical context, not a new audience. Never forward the whole handoff as a public report. They do not transmit credentials in arguments, retrieve extra private records or execute another tool. The host still decides whether to call; each call rechecks credential scope. REST continuation pointers remain available for REST clients.

1. Read claim_review.blocking, abstention_reason_codes, why_no_verdict and decision_authorization before presenting a recommendation. A blocking claim review or abstained status overrides any numeric candidate signal. Preserve all stated abstention reasons, not just the claim-review reason. Missing authorization fields are not permission.
2. When present, normalized scores.executive and scores.dimensions are 0-100 MVR score readouts, not 0-1 probabilities. For example, scores.executive.permission=57 means 57 score points, not a 57% probability of success or a licence-validity percentage. Executive values aggregate dimension evidence and need not equal the same-named dimension. Null or omitted values are unavailable, not zero. Do not multiply these scores by 100 or mix them with the separately scoped confidence fields.
3. permission is a modeled relational operating dimension, not confirmation that a statutory licence is valid or that a regulator authorized the proposed activity. Applicable numeric thresholds and sample gates are necessary where required, not sufficient for readiness or use of the output. Claim linkage, source review, geography, freshness, provenance, calibration, vetoes and other reported gates still constrain interpretation. Do not reconstruct a verdict from a mean score or hard-code one universal threshold across archetypes and profiles.
4. Respect diagnostic_only=true, not_a_verdict=true and decision_use=non_decisive_diagnostic_only wherever returned. Retained scores, intervals, per-class sample issues, sample_gate_passed and finite_population_review are diagnostic disclosures on a blocked result, not approval. Preserve calibration_basis and calibration/proxy limitations with the readout; a calibration disclosure or census declaration does not waive a gate. Blocked results may intentionally omit old board, reconciled, counterfactual and advisory branches. Never fill those omissions from an earlier response or promote a diagnostic branch to the headline.
5. Treat pilot_only as a candidate ceiling, not execution permission or a complete pilot plan. Neither pilot_ready nor ready_to_scale by itself authorizes action. Read decision_authorization.status, authorized_use, not_authorized_use, blocking_gates and human_review_required together with decision_reliability.not_authorized_for and any execution_authorization_state. Even authorized is bounded permission to use the API output for the listed purpose, not an autonomous launch, credit, investment, legal, regulatory, board or scale approval. A human-approved pilot still needs its actual scope, safeguards, owners and review conditions; do not invent them from the label.

Compact output is a projection, not a weaker safety contract: preserve its answer, authorization boundaries, claim review, abstention reasons and diagnostic/calibration disclosures. Do not interpret omitted bulky branches as passed checks. Neither a receipt/hash, deterministic replay, direct_support label, successful tool call nor strict_calibrated profile proves source truth, detects all fraud, or establishes empirical superiority over another method. Empirical performance claims need separately scoped evaluation evidence; this integration contract makes no such guarantee.

## Declared controls and review authority

Compatibility update 2026-09-08.engine-contract.10: shared container-conflict errors name the portable evidence_pack and compiled_pack keys without advertising an alias on routes that do not accept it. Licensed REST decision-check and evidence-completeness require evidence_pack or compiled_pack. Licensed REST context/compile additionally accepts evidence_items as its documented utility alias. For portable integrations use evidence_pack; do not assume that an alias supported by one route exists on another. Accepted inputs, conflict rejection, scoring and authorization are unchanged.

The same release repairs licensed presentation and intake parity. Nonterminal licensed MCP text is a minified handoff-first projection: status, continuation_disposition and the complete unchanged mcp_next_call come before diagnostics. Text omits duplicate continuation aliases, examples and bulky source/compiled copies; text_projection names omitted fields and structuredContent retains the full result. Blocking claim/evidence-fit details and input corrections remain in text. Terminal text retains the full result with interpretation and use boundaries early. This reduces host truncation risk; it does not guarantee every host's context budget or native completion. Do not execute an incomplete or truncated handoff.

execution_authorization_state is always not_authorized for decision-check. A decision_authorization.status of authorized can permit named uses of the analysis, never execution of the requested action. requested_judgement_output's board-use label is refreshed from final authorization, not the earlier draft. decision_interpretation.effective_use distinguishes permitted output uses, candidate verdict and the always-withheld external-action authority. Reliability exclusions, human review, diagnostic flags and all blockers still apply.

Typed survey_payload.sampling_method accepts census consistently with the finite-population contract. A census requires consistent positive integer population_size and sample_size declarations. Census is not automatically representative, independently verified, or exempt from weighted sample admission. Nested temporal.freshness_date and provenance_ledger.collection_date aliases are typed as strings in discovery. Geo coverage counts describe declared country matches without explicit local conflicts, not proof of exact local coverage; a regional record does not automatically satisfy a requested town/zone under strict tolerance. Never widen or rename scope solely to clear a gate.

Compatibility update 2026-09-08.engine-contract.9 completes the .8 telemetry repair: generic document/telemetry context belongs in the existing EvidenceItem.payload object, not telemetry_payload. The telemetry validation error now names that actual key. Strings, arrays, booleans, numbers and null are rejected for payload; a valid context object remains supported. The .9 follow-up also enforces the object contract at context/compile's separate raw-input validator, including evidence_items and compiled compatibility envelopes. No metric_name/observation_count/system_of_record telemetry schema is implied. Arbitrary context does not become observed numeric scores or authenticated evidence. Preserve evaluation-specific requirements such as evaluator_name and other applicable typed payload contracts.

Compiled items can now carry input_interpretation.notices code compiled_lane_differs_from_flat_inference, naming lane and inferred_lane from the existing flat-pack routing on the item's submitted metadata before container defaults. This is a non-blocking organizational hint, not proof of correct provenance or a command to move the item. An item may legitimately serve multiple collection purposes. Unknown items do not receive a guessed lane notice. Nothing is auto-reassigned or re-scored. Compiled evidence_missing_lanes and downstream recruitment/remediation still use declared container occupancy; these can differ from a flat submission of identical items. Review the existing evidence before collecting duplicates, preserve source facts, and never relabel merely to clear a missing-lane prompt. The notice does not establish source independence or assess undeclared prose.

Compatibility update 2026-09-08.engine-contract.7: successful licensed evidence responses include non-blocking input_interpretation.notices with code unrecognized_field for item-level names outside EvidenceItem, the public preflight item vocabulary and listed compatibility names (x-compatible-item-field-names). Each notice names the evidence item, container path and field, never its value. A unique one-edit spelling match supplies suggested_field only; the engine does not auto-correct, invent source geography, change scores or waive an existing gate. For example evidence_geograpy is reported with suggested_field=evidence_geography; correct the key only after checking the original source geography. HTTP 200 plus a notice is not evidence admission or authorization. Unknown nested keys in open metadata are outside this item-level check; closed control objects retain their existing validators.

Notices are bounded to 40 displayed entries, a maximum of the supported evidence-item count and 256 field names per item. scan_truncated means the input inspection was incomplete; notice_count then covers inspected inputs only, not every possible issue. Nonstandard names are redacted in notices. Normalization-generated internal fields are not caller typos. Extensible metadata remains accepted, and recognized names do not establish that every control applied to this particular decision. No source-authentication capability, score calibration, lane authority or caller entitlement was added.

Compatibility update 2026-09-08.engine-contract.5: provenance_ledger.data_integrity now has a closed, discoverable schema. Example fragment inside a complete evidence item: {"data_integrity":{"variance_anomaly_detected":true,"bot_or_farm_probability":0.95}}. variance_anomaly_detected is boolean, NOT a numeric anomaly score. bot_or_farm_probability is a finite number from 0 to 1. Unknown names and null now return correction errors instead of silently passing; correct clients need no migration. Omit the object when unavailable. An empty object or omission means no assessment supplied, not a passed fraud check. Do not invent clean values to satisfy validation.

These caller-declared upstream signals were already used by decision-check before this contract repair, in flat evidence_pack and compiled_pack. In client_safe inspect scale_recommendation.blueprint_codes and decision_authorization, including data_integrity_gate.blocking_alerts when blocked, not only confidence or pilot_only. data_quality.integrity_alerts is an internal/full diagnostic path and may be omitted by projection. The controls constrain decisions and institutional use but do not read adverse prose, authenticate originals, detect concealed source ancestry, verify a reviewer name or calculate bot probability. No scoring, sample or integrity threshold was changed by this release.

Compatibility update 2026-09-08.engine-contract.6: submit exactly one evidence container to decision-check, evidence-completeness and context/compile: evidence_pack OR compiled_pack. Even empty or equivalent dual containers return 422 AMBIGUOUS_EVIDENCE_CONTAINERS; neither wins silently. Nested compatibility envelopes cannot also contain competing lane maps. Submit one complete authoritative pack, preserving counterevidence and links, not whichever container produces a preferred answer. Server-internal compiler mirrors are not a second external input contract.

Declared population_size and sample_size must be positive safe integers. A declared sample cannot exceed its population, and a declared complete census requires equal counts. Conflicting top-level and collection_context declarations are rejected. Repeated observations are not additional population members. A valid census still supplies only scoped caller-declared context; it does not lower admission floors or authenticate the population. The EvidenceItem schema exposes these fields at item level and under collection_context.

Partial shared-source groups now produce non-blocking evidence_fit.issues code partial_shared_source_dependence. The existing all-one-source review gate is unchanged. Distinct source IDs still do not prove independence. input_interpretation discloses existing fractional-score normalization and a compiled lane whose explicit item type differs from its default. Lanes supply missing defaults, not authenticated provenance, and explicit item metadata continues to govern scoring. No new concentration or scoring thresholds were introduced.

The public cached /v1/capabilities document describes deployment capabilities, NOT the caller's entitlements, including when a credential is sent. access_scope makes that explicit. Plan, role, evaluation scope and record ownership are checked at the actual route; this release grants no new permissions. Error-response hashes are diagnostic content hashes, not registered proof: cite.verification_url and preferred_hash_kind are null for failed results. Success receipts keep their existing semantics; durable external citation should use governed snapshots.

EvidenceItem remains extensible for source metadata. Arbitrary additional keys do not create engine controls. Validate control objects against the published schema; do not infer that an accepted metadata key was evaluated. review_status uses its published enum and remains a caller declaration, not signer verification. Business use_case text describes context rather than selecting authorization. load_bearing=false cannot override a claim link that makes evidence relevant. Neither lock_pack nor a free-text review name grants stored-record authority.

Compiled input is an alternative container, not an exemption: retain the actual evidence type, provenance, source IDs and claim links when placing items into the documented lanes. Do not relabel an item to suit a lane. Every supplied lane is validated, including malformed data_integrity, before normalization. Context compilation is assembly, not a decision or fraud verdict. Coverage of flat requests alone does not establish coverage of compiled requests or the other licensed workflows.

Compatibility update 2026-09-08.engine-contract.3: load-bearing stance=opposed or hostile produces declared_adverse_stance_requires_review. The result keeps the supplied claim relationships unchanged: stakeholder opposition is a decision risk, not proof that a statement is false. An adverse stance is indexed by item and linked claim; existing contradiction, sample and authorization gates still apply. This change catches disclosed opposition, not unmarked adverse prose. Do not omit or relabel evidence to pass.

structured_values_provenance is keyed by the dimensions in that same item's structured_values. Example: {"trust":"observed","permission":"imputed"}. A nested input_score_provenance key never governed all values; it is now rejected instead of ignored. Provenance affects weight, admitted support and proxy caps. Inspect the full response, not only a verdict label that another gate may pin.

behavioral_values currently accepts only 30_day_active_retention_percent (0-100) on corporate telemetry. The implemented dissonance check pairs high consumer field permission with low telemetry retention under the configured maturity policy. It emits severe_evidence_dissonance_survey_vs_telemetry in scale_recommendation.blueprint_codes and caps escalation at pilot_ready. If the candidate was already pilot_only, the label need not change. Generic churn/retention/frequency names are rejected rather than guessed; trust and churn are not interchangeable constructs. This is a configured consistency screen, not proof of a causal relationship or an independently calibrated prediction.

uncertainty_envelope supports source_uncertainty, sampling_uncertainty, extraction_uncertainty, temporal_uncertainty, archetype_fit_uncertainty and disagreement_uncertainty, each 0-1. These components affect weights. interval_80, score_point_estimate and basis are descriptive input metadata, not calculated confidence bounds or gate overrides. A wide interval is not silently treated as a different uncertainty model. Unknown field names are rejected. guardian_tier describes an applicable guardian's role, not a universal confidence control; changing a role label alone need not change a score. Explicit temporal.expired=true on decision-relevant evidence requires review even when its source date is recent.

Human-review fields are caller declarations. reviewer_identity_verified=false and review_event_authenticated=false are explicit in AI-extraction policy disclosures. Typing a name is not a signature, reviewer consent, independently established identity, source independence or proof a review occurred. No blanket self_reported attestation requirement has been added: another checkbox would not solve hidden ancestry. Separately authorized persisted claim-review workflows remain separate from these intake declarations.

Projection correction 2026-09-08.engine-contract.4: claim_review.human_review_authority carries these non-authentication flags for declared-reviewed AI evidence in client, board, export and compact results. It includes no reviewer name or source contents. Internal-only policy metadata is not sufficient disclosure for a consuming agent.

Finite-population reproduction: call POST /v1/evidence-completeness (licensed MCP mvr_evidence_completeness uses arguments.payload) with a complete valid pack containing a field_research guardian item and collection_context {"sample_size":4,"population_size":4,"sampling_method":"census","is_complete_population":true,"population_definition":"All four identified guardians for the exact synthetic permission decision"}. Read /finite_population_review there. On decision-check inspect /local_relational_assessment/sample_sufficiency/finite_population_review or /field_sample_report/finite_population_review, including diagnostic projections; do not assume the same top-level pointer on every route. For blocked early scores the generic admission may be not_evaluated rather than a numerical pass. The complete maintainer-supplied synthetic request contains the other required context, source metadata and claim links; this small fragment alone is not a valid pack and does not waive any gate.

## Resume a corrected public pack

At an evidence pause, mcp_next_call is deliberately null. evidence_collection_plan lists missing-proof tasks, not external actions already performed. evidence_recovery is guidance requiring new input and user authorization, NOT a next call to execute unchanged. The free Evidence Builder at https://africanmarketos.com/mvr-evidence-builder/ can help the user prepare sources; it is optional.

The client retains the consented original context and entire pack. With corrected evidence for the same decision inside the original two-hour window, call mvr_evidence_completeness again with arguments.payload containing that full context, complete replacement pack, workflow_id and workflow_expires_at. No raw evidence is stored for retrieval or appended server-side. The ID is operational measurement, not authentication or an entitlement. After expiry, a changed decision or lost context, start mvr_first_call afresh. Each guided retry starts a fresh bounded sequence with its complete corrected input.

No Notion, Airtable or Jira connector is required. A client may show the structured task list to its user and, only after separate approval, copy a minimized version into their task system. AMOS does not do that automatically. Completion, a lead, checkout and payment remain different events. Optional paid routes never authorize purchase or replace evidence collection.

## Reproducible reports

Report endpoint, protocol version, UTC time, HTTP status, cf-ray, error code and a redacted minimal request. Do not share credentials or private evidence. A transient GET failure needs those identifiers to investigate; a current successful GET cannot disprove a past outage.

For host/model comparisons, isolate the intended connection and inspect the actual selected server and tool in the trace. Configuring a direct MCP URL does not prove the host used it if another connector exposes equivalent tools. Record both configured model IDs and any available backend snapshot, prompts, additional host policy, caching, repeated-run counts and full request/response traces. Include a policy-only control when testing whether AMOS adds value beyond instructions. Same-vendor model swaps are not cross-vendor validation; synthetic cases are not customer outcomes. Separate timing, total tokens, uncached tokens and actual price rather than interpreting token ratios as a bill.

Protocol references: https://modelcontextprotocol.io/specification/2025-11-25/server/tools and https://modelcontextprotocol.io/specification/2026-07-28/basic/transports/streamable-http
